# generated audio + candidates from local runs — never commit
examples/runs/
*.mp3
*.wav
*.caf

# reproducibility sidecars from local runs
*.voice_id
*.seed

# ...EXCEPT the curated reference samples, which are tracked on purpose
!examples/samples/
!examples/samples/**

# secrets — keys come from the environment, never the repo
.env
*.key
config.json

# os / editor
.DS_Store
__pycache__/
*.pyc
